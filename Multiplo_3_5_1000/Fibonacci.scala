object Fibo {

	/*def fib(anterior: Int, soma: Int): Int = {
		val x = anterior + soma
		printn("%d...".format(x))
		if (x < 4000000 ) {
			fib(soma, x)
		}		
	}*/

	/*def soma: Int = {
		val x = fib(0,1)
		if (x % 2 == 0) {
			x
		}
	}*/

	def main(args: Array[String]) {
		//println(fib(0.1))
	}
}